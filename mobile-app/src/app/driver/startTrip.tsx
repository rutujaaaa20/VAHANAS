import { View, Text } from "react-native";

export default function StartTripScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Start Trip Screen</Text>
    </View>
  );
}